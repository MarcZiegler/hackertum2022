# Running dev environment

### 1. `npm i`
Installs all the required dependencies.

### 2. Create and configure `.env` file (optional)
Configure environment variable files, all prefixed `REACT_APP`

### 3. `npm start`
Open [http://localhost:8080](http://localhost:8080) to see live changes in the browser.

# Deploying to production environment
TODO